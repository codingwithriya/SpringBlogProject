spring:
  application:
    name: SpringBlogProject

  datasource:
    url: jdbc:mysql://localhost:3306/blogpost
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081